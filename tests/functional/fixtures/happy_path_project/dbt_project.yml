analysis-paths:
- analyses
config-version: 2
flags:
  send_anonymous_usage_stats: false
  state_modified_compare_more_unrendered_values: true
macro-paths:
- macros
name: test
profile: test
seed-paths:
- seeds
models:
  +enabled: true
  test:
    +enabled: true
    sub:
      +enabled: true
      inner:
        +enabled: true
seeds:
  +quote_columns: true
  test:
    +quote_columns: false
snapshot-paths:
- snapshots
test-paths:
- tests
